; ============================================================
; GTCRN-AENR training config
;
; Usage: python3 train.py --config config.ini   (see train.py's top-of-file
; docstring for the full CLI and for how to render/pack the dataset this
; file points at).
; ============================================================

[signal]
; GTCRN-AENR is LOCKED to upstream 16 kHz / 512/512/256 -- the model itself
; (GTCRNAENR.__init__) raises "upstream 16 kHz/512..." on any other grid.
; Do not try to run this candidate at 48 kHz; use one of the other five
; candidates for that, or GTCRNAENR's README for what a 48 kHz extension
; would require.
sr = 16000
n_fft = 512

[data]
; Output of (--output is 16 kHz-specific -- DeepFilterNet-AENR's 48 kHz
; generation writes into its own data_aec_48k/, and the two MUST NOT share a
; directory: whichever sample rate was generated last would silently
; overwrite the other's seqs/packed content):
;   python3 -m AIAEC.dataset_gen.gen_aec_dataset --config ... --split all --hours 100 --output data_aec_16k
;   python3 -m AIAEC.dataset_gen.pack_aec_dataset --input data_aec_16k/all --output data_aec_16k/packed/all
; ONE unified pool. Training applies a deterministic random per-chunk split;
; evaluate source generalisation on a separate held-out corpus.
packed_dir = data_aec_16k/packed/all
; Ordinary shuffled model batches. PBFDKF has already been materialized as
; dataset channel six and is never rerun by the trainer.
batch_size = 8
num_workers = 4
; Held out at LOAD time by deterministic per-chunk random split.
; 0 = no validation pass (not recommended -- early stopping and *_best.pth
; both need one).
val_fraction = 0.1

[model]
; Every key here is a keyword argument of GTCRNAENR.__init__ (model.py).
; Commented values below ARE the constructor defaults.
; erb_subband_1 = 65
; erb_subband_2 = 64

[training]
output_dir = output
lr = 1e-3
weight_decay = 1e-4
amsgrad = true
max_epochs = 100
early_stop_patience = 15
grad_clip = 1.0

[loss]
; See AIAEC/training_common.py's compressed_spectral_loss: no candidate paper
; publishes a loss, so all six trainers share this one for comparability.
compression = 0.3
magnitude_weight = 1.0
complex_weight = 1.0
