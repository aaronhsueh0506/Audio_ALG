# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
*.egg

# Virtual Environment
venv/
env/
.venv

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# macOS
.DS_Store

# Audio files (generated)
*.wav
!test_*.wav

# Output
output/

# AINR training data & model checkpoints
ainr/RNNoise-ERB/output/
*.pth
*.onnx
*.npy

# Logs
*.log
*.tmp

# Claude Code
.claude/

# machine-specific built binary (regenerable via make in pipelines/)
pipelines/aec_nr_pipeline

# build artifacts (extensionless pipeline binaries + objects)
pipelines/aec_nr_pipeline
pipelines/aec_nr_pipeline_static
pipelines/test_audio_pipeline
pipelines/libaudio_pipeline.a
lib/hpf/obj/
*.o

# pipelines/Makefile's own hash-keyed object dir (obj/<backend>-<config-hash>/,
# one subdirectory per distinct BACKEND/EXTRA_CFLAGS/WERROR combination --
# see the OBJ_DIR/CFG_SIG comment in that Makefile)
pipelines/obj/
